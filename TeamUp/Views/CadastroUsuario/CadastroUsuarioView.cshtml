@model TeamUp.ViewModels.CadastroUsuarioViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section title{ Cadastro de Usuário }

@section Body{
    
    <form method="post" enctype="multipart/form-data" action="@Url.Action("EfetuaCadastro")">

        @Html.TextBoxFor(m => m.Email)
        @Html.ValidationMessageFor(m => m.Email)

        @Html.PasswordFor(m => m.Senha)
        @Html.ValidationMessageFor(m => m.Senha)

        @Html.PasswordFor(m => m.SenhaConfirmacao)
        @Html.ValidationMessageFor(m => m.SenhaConfirmacao)

        @Html.TextBoxFor(m => m.PrimeiroNome)
        @Html.ValidationMessageFor(m => m.PrimeiroNome)

        @Html.TextBoxFor(m => m.UltimoNome)
        @Html.ValidationMessageFor(m => m.UltimoNome)

        <input name="ImagemPerfil" type="file" />

        <input type="submit" />

    </form>

}
